/* the implementation for the function signatures in your
header file */


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Node.h"

/*Node* createNode(int nodeID, int connCount){
Node* node = (Node*)malloc(sizeof(Node));
node->nodeID = nodeID;
node->connCount = connCount;
node->listConn = (int*)malloc(sizeof(int)*connCount);
return node;
}*/

/*void destroyNode(Node* node) {
  free(node->nodeID);
  free(node);
}*/

